# CrowdOrganHost build configuration.
#
# Drop overrides in here if you need custom compiler flags for gestures.
# Everything stays stock openFrameworks by default so the CI and your
# laptop agree about what "builds" means.

